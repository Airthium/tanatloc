ThreeJS :
- rendering order for sprites

Converters:
- Support geometry names with space

Result:
- Bas file name sometimes

Materials:
- dirty UI

Models:
- compare current model with loaded one (without values)