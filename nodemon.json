{
  "watch": ["src/server"],
  "exec": "swc src --out-dir dist/src && swc config --out-dir dist/config && swc plugins --out-dir dist/plugins && swc templates --out-dir dist/templates && node dist/src/server/index.js",
  "ext": "js ts"
}
