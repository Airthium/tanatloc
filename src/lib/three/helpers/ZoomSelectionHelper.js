import { SelectionHelper } from 'three/examples/jsm/interactive/SelectionHelper'

const ZoomSelectionHelper = (renderer) => {
  const selectionHelper = new SelectionHelper(null, renderer, 'SelectionHelper')
}

export { ZoomSelectionHelper }
