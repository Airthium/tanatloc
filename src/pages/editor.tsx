/** @module Pages.Editor */

import EditorPage from '@/components/editor'

/**
 * Editor
 */
const Editor = (): JSX.Element => {
  /**
   * Render
   */
  return <EditorPage />
}

export default Editor
