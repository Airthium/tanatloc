/** @module Pages.Editor */

import Editor from '../components/editor'

/**
 * Editor page
 */
const _editor = (): JSX.Element => {
  /**
   * Render
   */
  return <Editor />
}

export default _editor