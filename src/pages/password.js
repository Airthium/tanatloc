import PasswordPage from '@/components/password'

/**
 * Password recovery
 * @memberof Pages
 */
const Password = () => {
  /**
   * Render
   */
  return <PasswordPage />
}

export default Password
