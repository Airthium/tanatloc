import PasswordPage from '@/components/password'

/**
 * Password recovery
 * @memberof module:pages
 */
const Password = () => {
  /**
   * Render
   */
  return <PasswordPage />
}

export default Password
