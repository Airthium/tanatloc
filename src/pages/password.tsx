import PasswordPage from '@/components/password'

/**
 * Password recovery
 * @memberof Pages
 */
const Password = (): JSX.Element => {
  /**
   * Render
   */
  return <PasswordPage />
}

export default Password
