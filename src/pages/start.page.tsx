/** @module Pages.Start */

import StartPage from '@/components/start'

/**
 * Start
 */
const Start = (): React.JSX.Element => {
  /**
   * Render
   */
  return <StartPage />
}

export default Start
