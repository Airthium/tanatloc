import { ThemeConfig } from 'antd/es/config-provider/context'

const colorPrimary = '#fad114'
const textColor = '#1f1f1f'
const colorLink = '#ee9817'

const theme: ThemeConfig = {
  token: {
    colorPrimary: colorPrimary,
    fontFamily: 'Saira',
    borderRadius: 0,
    colorText: textColor,
    colorBgLayout: '#fff',
    colorLink: colorLink
  }
}

export default theme
