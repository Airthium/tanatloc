// Fespaces
if (mpirank == 0) cout << "Define finie element space..." << endl;
if (mpirank == 0) cout.flush;

<% const FESpace = finiteElementSpace.value || finiteElementSpace.default; -%>
fespace <%= finiteElementSpace.name -%>(<%= mesh.name -%>, [<%= FESpace.split(',') -%>]);
