// Fespaces
appendLog("Define finie element space...");

<%
    const FESpace = locals.finiteElementSpace.value
        || (
            locals.dimension === 2
                ? (
                    locals.finiteElementSpace.default2D
                    ??
                    locals.finiteElementSpace.default
                ) : locals.finiteElementSpace.default
            )
-%>
fespace <%- locals.finiteElementSpace.name -%>(<%- locals.mesh.name -%>, [<%- FESpace.split(',') -%>]);
// End Fespaces