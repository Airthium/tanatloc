// Headers
if (mpirank == 0) {
    // Log file
    ofstream logOut("process_log.log");

    // Date
    logOut << "Starting date: " << "<%= new Date().toLocaleString('en-GB', {timeZone: 'UTC'}) -%>" << endl;
    
    // Process file
    ofstream processOut("process_data.log");
}
