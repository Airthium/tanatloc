// Headers
int v = verbosity;
verbosity = 0;

if (mpirank == 0) cout << "Starting date: " << "<%= new Date().toLocaleString('en-GB', {timeZone: 'UTC'}) -%>" << endl;
if (mpirank == 0) cout.flush;

verbosity = v;
