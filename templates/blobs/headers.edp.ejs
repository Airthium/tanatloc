// Headers
if (mpirank == 0) {
	// Log file
	ofstream logOut("process_log.log");

	// Date
	logOut << "Starting date: " << "UTC <%- new Date().toLocaleString('en-GB', {timeZone: 'UTC'}) -%>" << endl;

	// Process file
	ofstream processOut("process_data.log");
}

func int appendLog (string str) {
	if (mpirank == 0) {
		// Log file
		ofstream logOut("process_log.log", append);

		logOut << str << endl;
	}
}

func int appendWarning (string str) {
	if (mpirank == 0) {
		// Log file
		ofstream logOut("process_log.log", append);

		logOut << "Warning:" << endl;
		logOut << str << endl;
	}
}

func int appendError (string str) {
	if (mpirank == 0) {
		// Log file
		ofstream logOut("process_log.log", append);

		logOut << "Error:" << endl;
		logOut << str << endl;
	}
}

func int appendPercent (int percent) {
	if (mpirank == 0) {
		// Log file
		ofstream logOut("process_log.log", append);

		logOut << "PERCENT: " << percent << endl;
	}
}

appendPercent(0);
// End Headers