<%- include('macro_gradient.edp.ejs'); -%>
<% if (locals.dimension === 2) { -%>
macro Grad(A) [grad(A[0]), grad(A[1])] //
<% } else { -%>
macro Grad(A) [grad(A[0]), grad(A[1]), grad(A[2])] //
<% } -%>