// Materials
if (mpirank == 0) {
    ofstream logOut("process_log.log", append);
    logOut << "Define materials..." << endl;
}

<% materials.children.map(child => { -%>
<% const value = child.value || child.default -%>
real <%= child.name %> = <%- value -%>;
<% }) -%>